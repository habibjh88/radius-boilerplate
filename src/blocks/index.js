/**
 * Block editor entry.
 *
 * Registers the editor UI for blocks whose markup is rendered in PHP
 * (Blocks\BlockManager::render_items_block), so the front end never ships the
 * editor bundle.
 */
import './items';
