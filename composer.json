{
    "name": "radiustheme/radius-boilerplate",
    "description": "WordPress plugin boilerplate with a DI container, ORM, migrations, REST router and WP-CLI scaffolding.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "authors": [
        {
            "name": "RadiusTheme",
            "email": "support@radiustheme.com"
        }
    ],
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    },
    "autoload": {
        "psr-4": {
            "RadiusTheme\\RadiusBoilerplate\\": "includes/"
        }
    },
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "dealerdirect/phpcodesniffer-composer-installer": "^1.0",
        "phpcompatibility/phpcompatibility-wp": "^2.1",
        "phpunit/phpunit": "^9.6",
        "wp-coding-standards/wpcs": "^3.0",
        "wp-phpunit/wp-phpunit": "^6.4",
        "yoast/phpunit-polyfills": "^2.0"
    },
    "scripts": {
        "phpcs": "./vendor/bin/phpcs --standard=phpcs.xml",
        "phpcs:fix": "./vendor/bin/phpcbf --standard=phpcs.xml",
        "phpcs:report": "./vendor/bin/phpcs --standard=phpcs.xml --report=csv > phpcs-report.csv",
        "test": "phpunit",
        "phpunit": "phpunit"
    }
}
