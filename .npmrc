# Several @wordpress/* packages still declare a `react@^18` peer range even
# though WordPress 6.8+ ships React 19. Installing with legacy peer resolution
# avoids that stale-peer conflict; the runtime React is whatever WordPress
# serves, since react/react-dom are externalised by the dependency-extraction
# plugin and never bundled.
legacy-peer-deps=true
